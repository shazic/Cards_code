[pytest]
markers =
  smoke: subset of tests
  exception: check for expected exceptions
  finish: subset of tests
  num_cards: number of cards to prefill for the cards_db fixture
addopts = 
  --strict-markers